# Copy this to .env.local for local development.
# For production CI, set these as GitHub secrets.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Supabase Storage setup:
# 1. Create a "projects" bucket (private) for user project files
# 2. Create a "shared" bucket (public) for shared project views
# 3. Add RLS policies — see SQL templates in the repo docs
